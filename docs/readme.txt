

"application-prod.yml" is default config

note:
If the default options do not match, please use "docs/pc-print-conf" option, 
Copy the optional file to overwrite the default

To edit an ".yml" file, use the "docs/nodepad + +" program
